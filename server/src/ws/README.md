# ws

Socket.io handlers, rooms and role projection (`specs/02-architecture.md` §1). Empty until LIV-01.

The conventions it will use exist since FND-03: the envelopes, channel names and acknowledgement type in `shared/src/live.ts`, and command validation in `server/src/domain/commands.ts` (D-064).
