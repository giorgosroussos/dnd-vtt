.doc-unlock
__pycache__/
.claude/settings.local.json

# dependencies and build output
node_modules/
dist/
*.tsbuildinfo

# local development environment (make setup copies .env.example)
.env
.dev-data/

# Playwright output
e2e/test-results/
e2e/playwright-report/

# Windows download metadata that WSL shows as a file; ':' is not a legal Windows
# file name character, so a tracked one breaks every Windows checkout.
*:Zone.Identifier
