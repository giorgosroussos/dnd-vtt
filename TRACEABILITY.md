# TRACEABILITY

Implementation status and evidence per work package (`specs/13-implementation-plan.md`) and per critical journey (`specs/10-testing-acceptance.md` §5). Status values: `not started`, `in progress`, `done`. Status is set only from evidence that ran and passed; never from plans, file presence or stubs. `make check-docs` verifies that every package has exactly one row and that `done` rows carry evidence. The product-intent scope matrix is `specs/11-traceability.md`.

## Work packages

| Package | Phase | Outcome | Key specs | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| FND-01 | 0 | Command contract and repository scaffold | `02` §1, `09` §1 | done | 2026-09-23, Linux (WSL2), Node 24.11.0. `make clean-start` exit 0: a copy of the working tree with an empty temporary data directory ran setup (`npm ci`), infra-up, migrate, verify, then `make smoke` against `make dev`, and was removed. `make verify` exit 0: lint, format-check, typecheck, `make test` (51 Vitest tests, among them `server/src/db/migrate.test.ts` "migrateDataDirectory against a real SQLite file" in a temporary data directory, and `server/src/http/app.test.ts` "answers 404 at /api/…"), `make e2e` (2 Playwright tests in `e2e/tests/views.spec.ts`: a DM context and a player context against the production build), build, check-docs. `make smoke` exit 0 against `make dev` and against `npm start`. `make audit` found 0 vulnerabilities. `make scan-secrets` exit 0, and exit 1 with a planted GitHub token. `make help` lists all 25 targets of AGENTS.md "Commands"; `make infra-up`, `infra-status` and `infra-down` exit 0. `npm start` and `make test` refuse Node 22 and Node 12 with a message. |
| FND-02 | 0 | CI baseline | `10` §4, `10` §2 | done | 2026-09-23, pull request https://github.com/giorgosroussos/dnd-vtt/pull/1, `.github/workflows/ci.yml`. Green: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35906565756 (commit `ed05102`) and https://github.com/giorgosroussos/dnd-vtt/actions/runs/35907555101 (commit `1649fa8`, the same tree): all 23 jobs passed, each running one `make <target>`: `lint`, `format-check`, `typecheck`, `test` (67 Vitest tests against real SQLite files in temporary data directories), `e2e` (2 Playwright tests, DM and player contexts against the production build), `build`, `check-docs`, `audit`, `scan-secrets` on `ubuntu-latest` and on `windows-latest` (MSYS2 `make`, native Node 24), plus the five tripwire jobs, each printing "gate ABSENT" and its promotion condition. Red: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35907022399 (commit `88f76c3`, a deliberately failing `shared/src/deliberate-failure.test.ts`) turned `test · linux` and `test · windows` red on that assertion while the other 21 jobs passed; reverted in `1649fa8`. Earlier red runs found and fixed three real defects: `make build` on a fresh checkout (missing project reference to `shared`), a POSIX-only path in `server/src/config.test.ts`, and Vite's dev-server close hanging during its first dependency optimization. Local: actionlint with shellcheck exit 0; a planted `@gate:hidden-information` marker turned `make tripwire GATE=hidden-information` red with promotion steps. Branch protection, 2026-09-23: ruleset 23898009 on `main`, read back from `GET /repos/giorgosroussos/dnd-vtt/rules/branches/main`, requires all 23 checks by name (the 18 gate jobs and the 5 tripwires) and blocks force pushes and deletion, so a red pipeline blocks a merge. Review (prompt 2), 2026-09-23: no critical or high finding; the medium and low findings were fixed (D-060, D-061 supersede D-058, D-059): the tripwire scans every text file, not only JavaScript and TypeScript; `scripts/ci-consistency.test.mjs` ties the `verify` prerequisites, both CI matrices, the tripwire registry and `README.md` together and fails when a required gate is neither a tripwire nor present (each checked by a deliberate mutation that turned it red); secretlint runs without `cmd.exe`; the Vite close wait is bounded; checkout no longer persists the token. After the fixes, locally: `make verify` exit 0 with 82 Vitest tests (29 in `scripts/`) and 2 Playwright tests, `make audit`, `make scan-secrets` (a planted token in `planted a%OS%b & c.txt` exits 1), `make tripwire` and actionlint exit 0; on GitHub, https://github.com/giorgosroussos/dnd-vtt/actions/runs/35911745219 (commit `1b98c60`) green on all 23 jobs. Not covered here: `make dev` and `make clean-start` on Windows (G-003). |
| FND-03 | 0 | API and WebSocket conventions | `02` §5, `04` §2, `04` §3, `04` §5 | done | 2026-09-24, pull request from branch `fnd-03-api-ws-conventions`. Green: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35927265858 (commit `6046a65`): all 23 jobs passed, `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on `ubuntu-latest` and `windows-latest`, plus the five tripwires (gates absent). Review (prompt 2), 2026-09-24: correctness, security and isolation, and test passes (no UI, so no UX pass). Two high findings, both fixed: `scrub` backtracked in roughly cubic time (4,000 characters took 9 s; now linear, `server/src/log/logger.test.ts` "scrubs adversarial text in linear time"), and a refused rename during rotation deleted the log history (`server/src/log/logger.rotation.test.ts`, both tests red against the old rotation). The medium and low findings were fixed with tests: the query string in a bad-URL `reason`, gaps in the redaction patterns, error-handler edge cases, detail pointer escaping, binary values, console control characters, strict payload schemas and `ownProperties` (D-066 and D-067 supersede D-065 and D-062). Two findings are deferred to SRV-02 as G-005 and G-006. After the fixes, locally: `make verify` exit 0 (210 Vitest tests, 2 Playwright tests), `make audit` 0 vulnerabilities, `make scan-secrets` exit 0, `make tripwire` all five absent. Fix commit `0694ecc` green on GitHub: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35972311648, all 23 jobs on Linux and Windows. The WebSocket half is evidenced in-process (`dispatchCommand` returns the envelope); no Socket.io transport exists until LIV-01 (G-001). Locally, Linux (WSL2), Node 24.11.0: `make verify` exit 0: lint, format-check, typecheck, `make test` (149 Vitest tests), `make e2e` (2 Playwright tests), build, check-docs. REST envelope and validation: `server/src/http/errors.test.ts` "answers an unknown /api path with 404 in the envelope", "answers a body with %s with 400 validation_failed and changes nothing" (wrong type, string not coerced, missing field, unknown field not stripped, below minimum), "answers malformed and empty JSON with 400 malformed_body", "answers an unsupported media type with 415", "answers a server failure with 500 internal_error and no detail of the failure", each response checked against `ErrorEnvelopeSchema` exported from `shared`; `server/src/http/app.test.ts` "answers 404 at %s in the error envelope". Commands and versions: `server/src/domain/commands.test.ts` "rejects %s in the error envelope and changes nothing" (12 cases: the target state deep-equal before and after, the apply step never called, the version counter not advanced), "refuses %s as unsupported" for all ten command types; `server/src/domain/version.test.ts` "starts at 1 and strictly ascends by one"; `shared/src/live.test.ts` checks the command and event names against the tables of `04` §2 and §3. Logging against a real temporary data directory: `server/src/log/logger.test.ts` (console and `logs/emberglass.log`, rotation keeping exactly `.1`–`.3`, redaction); `server/src/http/errors.test.ts` "keeps a PIN, a session cookie and a session identifier out of the console and the log file", which turned red when the string scrubbing was disabled as a deliberate mutation. `make audit` 0 vulnerabilities; `make scan-secrets` exit 0; `make tripwire` all five gates absent. `make smoke` exit 0 against `make dev` (`.dev-data/logs/emberglass.log` received `server.started`) and against the production build started with a temporary data directory, whose log file received `server.started` and a redacted `http.rejected` line. |
| FND-04 | 0 | Design, accessibility and localization foundation | `02` §2, `08` §6, `08` §8, `02` §6 | done | 2026-09-24, pull request https://github.com/giorgosroussos/dnd-vtt/pull/3 from branch `fnd-04-design-foundation`. Green: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35979443019 (commit `59cf2c6`), read back from the GitHub API: all 23 jobs passed, `lint`, `format-check`, `typecheck`, `test`, `e2e` (the keyboard smoke test among them), `build`, `check-docs`, `audit`, `scan-secrets` on `ubuntu-latest` and `windows-latest`, plus the five tripwires (gates absent). Review (prompt 2), 2026-09-24: correctness, security and isolation, tests, UX and accessibility. No critical or high finding. Fixed, with tests: the global `[tabindex='-1']:focus` rule removed the focus ring from any arrow-key-focused item, not only the skip link's landmark (now scoped to `main`; `e2e/tests/keyboard.spec.ts` "an element outside the Tab order still shows the focus ring when the keyboard moves to it", red against the old rule); the UI text scan missed a `children` prop, `createElement` arguments and a submit input's value (D-073 supersedes D-070; planted cases in `client/src/ui/messages.test.tsx`); the keyboard gate recorded activation in the page, so a link to another page would have failed it falsely (now recorded in the test process; a planted `/dm/elsewhere` link passed); the idle name sat at the top edge of the TV instead of its centre (`e2e/tests/views.spec.ts` idle test now checks fill and centring, red against the old CSS); the player-isolation check matched chunk names only (it now reads every script the player view loaded for DM markup); a catalogue text of placeholders only would have made the rendered-view check vacuous (now refused); the title plugin now HTML-escapes. Deferred: loading, reconnecting and load-failure states (G-007, LIV-01). No hidden token, prep scene, image, PIN, session or upload exists in this slice; `server/`, `shared/`, `scripts/` and CI are unchanged. After the fixes, `make verify` exit 0 (277 Vitest tests, 7 Playwright tests), `make audit` 0 vulnerabilities, `make scan-secrets` exit 0, `make tripwire` all five absent. Before the review: Locally, Linux (WSL2), Node 24.11.0: `make verify` exit 0: lint, format-check, typecheck, `make test` (269 Vitest tests in 19 files), `make e2e` (6 Playwright tests against the production build), build, check-docs. Catalogue (`08` §6, D-070): `client/src/ui/messages.test.tsx` "finds no UI text outside the catalogue in the client sources", "finds no UI text in index.html: the title and language come from the catalogue", "flags %s" (14 planted JSX cases) and "flags text given to %s in a .ts file" (5 cases), "show only catalogue text: %s" (both views and both fallbacks rendered in jsdom), "never names the trademark the product must not carry". A planted `<p>Hi</p>` in `client/src/dm/DmView.tsx` turned the static scan and the rendered-view check red; reverted. Keyboard and contrast (`08` §8, D-072): `e2e/tests/keyboard.spec.ts` "every interactive element of the DM view is reached, shown and operated by keyboard alone" and "every text in both views keeps readable contrast", in `make e2e`, so in the CI `e2e` job on both runners; `client/src/ui/tokens.test.ts` (palette pairs at 4.5:1, focus ring at 3:1). Removing the focus outline turned the keyboard test red; a muted text colour of 1.87:1 turned the contrast test red; both reverted. Shells and patterns (`02` §2, D-069): `e2e/tests/views.spec.ts` "a DM view and a player view open side by side from one server" (title and `lang` from the catalogue), "the player view is the idle screen: dark, the product name only, no control", "the player view does not load the DM view code"; `client/src/ui/components.test.tsx` (TextField error wiring, Notice, SkipLink focus, ErrorBoundary never shows the error text, Reload, the player view and its fallback have no focusable element). Offline assets (`02` §6, D-071): `e2e/tests/views.spec.ts` "both views request nothing but the local origin, their font and icon included" (the woff2 files answer 200 from the server, the page renders with Inter Variable, the declared SVG icon is local); adding a Google Fonts `@import` to `client/src/ui/tokens.css` turned it red; reverted. This is not the REL-02 `offline-e2e` or `external-url-build` gate. `make tripwire` all five gates absent; `make audit` 0 vulnerabilities; `make scan-secrets` exit 0; `make smoke` exit 0 against `make dev`, whose `/dm` page carries the catalogue title and `lang` and serves `/favicon.svg` as `image/svg+xml`. |
| SRV-01 | 1 | Schema and migrations | `03` §1, `03` §2, `03` §3, `03` §6 | done | 2026-09-24, pull request https://github.com/giorgosroussos/dnd-vtt/pull/4 from branch `srv-01-schema-migrations`. Review-fix commit `ecea756`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35985868379 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on `ubuntu-latest` and `windows-latest`, plus the five tripwires (gates absent). Commit `5cd8e67`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35984255365 green on all 23 jobs (Linux and Windows gates, five tripwires absent). Review (prompt 2), 2026-09-24: correctness, security and isolation, and test passes (no UI, so no UX pass). One high finding, fixed: two processes migrating one file both applied the same migration, because the version was read before a deferred transaction; each migration now takes the write lock and re-reads the version (`server/src/db/migrate.test.ts` "applies a migration once when a second process migrates the same file at the same time", a second process started through tsx while the first holds the lock; red against the old runner, which applied the migration twice). Medium and low findings fixed with tests (D-075 supersedes D-074): REAL columns refuse infinity ("refuses an infinite number in every REAL column, which JSON cannot carry"), a map-less scene cannot hold a square size, `settings.id` cannot change, a target below the current version has its own message, a failed backup applies nothing; every column's type, nullability and default pinned ("declares every column with the type, nullability and default of D-075"); CHECK constraints on labels, flags, names, tags, heights, variants, the PIN hash, square size and order, the preset insert trigger and tag uniqueness now covered; eight mutations that survived the first version now fail a test. The forward-migration path is exercised through an additive probe migration ("migrates the fixture forward through a later migration, after a backup, keeping every row"), because at version 1 the test on the repository migrations applies nothing and only guards later ones. Deferred: G-008 (PIN hash in settings reads and backups, SRV-02) and G-009 (tag case, variants shape, SRV-05 and SRV-04). After the fixes, locally: `make verify` exit 0 (335 Vitest tests, 7 Playwright tests). Before the review, locally: Linux (WSL2), Node 24.11.0, SQLite 3.53.4 (better-sqlite3): `make verify` exit 0: lint, format-check, typecheck, `make test` (325 Vitest tests in 21 files), `make e2e` (7 Playwright tests, whose production server applies migration 0001 to a fresh data directory at start), build, check-docs. Schema (`server/migrations/0001_initial_schema.sql`, D-074), all against real SQLite files in temporary data directories, in `server/src/db/schema.test.ts`: "creates exactly the eight entities of specs/03-domain-model.md §1" and "stores every key field of specs/03-domain-model.md §1 and nothing else but the id" (both read the table in `03` §1), "has exactly the relationships of specs/03-domain-model.md §2, with the deletion rules of §7", "keeps sessions and scenes in an order unique within their parent", "indexes every foreign key", "keys every table by one text id and has no sequential key" (STRICT, WITHOUT ROWID, no `rowid`, no `sqlite_sequence`), "creates the one settings row with a version 4 UUID and the default settings", "a second run applies nothing and changes nothing" (database file byte-identical, no backup). Constraints: "refuses a token without a scene or without an asset", "accepts only square grids, on scenes and on image presets", "accepts only the 5e-2014 rules version", "keeps character_id empty", "stores a map-less scene as columns × rows, defaulting to 30 × 20", "keeps token positions in decimal grid units, exactly" (text in `x` refused; recalibration leaves positions unchanged), "refuses %j as the id of every UUID-keyed entity" (6 cases), "refuses %j as an image id" (4 cases), "holds an image preset wholly absent or wholly present and calibrated", enumerations, session dates, one settings row. Deletion rules: cascades from session and campaign, the live scene cleared when it or an ancestor is deleted, an asset in use and an image in use refused. Fixture database (`server/src/db/testing/fixture.ts`, generated, at schema version 1): "migrates the fixture to the latest version, keeping every row and every reference" (applies nothing while 0001 is the only migration) (integrity_check ok, foreign_key_check empty, records unchanged) and "describe every row of the generated fixture database" (each row validated against the `shared` contract by the server's strict Ajv). Contract: "name exactly the stored columns, except the PIN hash, which never leaves the server"; `shared/src/entities.test.ts` (categories and sizes read from `05` §1–§2, schema accept/refuse cases, no `pin_hash` in Settings). Runner (`server/src/db/migrate.test.ts`): "rolls back a migration that leaves a row without its parent", "rebuilds a parent table without cascading to its children, as SQLite's procedure requires", "stops at a target version when asked", "enforces foreign keys on every connection it opens". Deliberate mutations, each reverted: leaving foreign keys on during migrations turned the two runner tests red; dropping the `character_id` trigger and STRICT on `token` turned three schema tests red; the first draft of the preset CHECK let a NULL size through and the preset test caught it before the fix. Removing the `foreign_keys = ON` pragma from `openDatabase` turns nothing red, because better-sqlite3 is built with `SQLITE_DEFAULT_FOREIGN_KEYS=1`; the pragma keeps enforcement independent of that build option. `make migrate` on an empty data directory: `schema version 0 -> 1 (applied 0001_initial_schema.sql)`, then `1 -> 1 (nothing to apply)`. `npm start` on an empty data directory created a version 1 database with the eight tables before listening, and `make smoke` exit 0 against it. |
| SRV-02 | 1 | PIN, DM session and guessing protection | `07` §1, `07` §2, `07` §6, `07` §7 | done | 2026-09-24, pull request from branch `srv-02-pin-session`. Review-fix commit `d4e4b0b`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35992654818 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on `ubuntu-latest` and `windows-latest`, plus the five tripwires (gates absent); `test · windows` and `e2e · windows`, red on the first run, now pass. CI on commit `9e140eb`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35990134838 red on `test · windows` (5 s timeouts in `app.test.ts` and `schema.test.ts`: scrypt at the production cost starved the two-core runner) and `e2e · windows` (EPERM removing the e2e data directory the server still held open); the other 21 jobs green. Review (prompt 2), 2026-09-24: correctness, security and isolation, and test passes (no UI, so no UX pass). One high finding, fixed: a sign-in with the old PIN still being hashed when a PIN change committed kept its session (reproduced; "lets no session made with the old PIN outlive a change, even one whose PIN check was still running", red against the old code, 5 of 5 green with the fix). Medium, fixed: setup reachable through DNS rebinding ("refuses setup from a loopback address whose Host is %s (DNS rebinding), with a matching Origin, and stores nothing", 4 cases, and an unparsable Host; red when setup checks the address only); encoded and dot-segment /api paths told routes apart without a session (eight spellings added to the identical-answer test, red without normalisation; `normalizedPath` unit cases). Tests added: "lets exactly one of two PIN changes at the same moment win, and ends the other device" (red without the compare-and-swap). Both CI failures fixed (D-077): cheap test hashes except where the cost is under test; a Windows-tolerant e2e teardown. Deferred: G-010 (lockout bypass by changing address, owner decision, REL-01) and G-011 (sockets of ended sessions, LIV-01); low findings noted in the review (HEAD on the public GET routes answers 401; cookies are shared across ports of one host, for the REL-01 README). After the fixes, locally: `make verify` exit 0 (438 Vitest tests, 7 Playwright tests); `server/src/http/auth.test.ts` 83 tests in about 15 s. Before the review: `make verify` exit 0: lint, format-check, typecheck, `make test` (421 Vitest tests in 25 files), `make e2e` (7 Playwright tests), build, check-docs. `make audit` 0 vulnerabilities; `make scan-secrets` exit 0, and secretlint exit 0 on the 14 new untracked files; `make tripwire` all five absent. All against real SQLite files in temporary data directories, `server/src/http/auth.test.ts` (66 tests, D-076). Setup (`07` §1): "succeeds from a loopback address, stores a salted scrypt hash and never the PIN, and signs that browser in" (the database file checked for the PIN), "accepts setup from the loopback address %s" (4 cases), "refuses setup from the LAN address %s before reading the body, and stores nothing" (5 cases, malformed and XML bodies included), "refuses setup once a PIN is set, from the server machine too, and keeps the hash", "lets exactly one of two setups at the same moment win", "refuses a PIN with %s and stores nothing" (9 cases: 3 and 9 digits, letters, spaces, newline, full-width and Arabic-Indic digits, empty, a number), "accepts the PIN %s at the edge of 4 to 8 digits". Session (`07` §2): "gives a correct PIN an HttpOnly, SameSite=Strict cookie carrying a random 256-bit identifier", "tells a browser only whether it itself holds a DM session", "replaces the session a browser already had when it enters the PIN again", "derives the role only from the session, never from anything the client declares" (`07` §7), "ends that browser session only when it signs out", "ends every session at a restart, keeps the PIN, and never writes a session to disk" (every file of the data directory searched for the identifiers). Lockout (`07` §6): "locks one address for 1 minute after 5 failures, even for the right PIN, and leaves another address alone", "doubles the lockout on each further run of 5 failures" (60, 120, 240, 480 s), "forgives earlier failures once the right PIN is entered", "checks no more than 5 PINs from one address when guesses arrive all at once" (20 parallel guesses: 5 checked, 15 refused), "counts a wrong current PIN in a PIN change towards the same lockout", "logs each failed attempt and each lockout with the client address". Change and reset: "ends every other session at once and keeps the device that changed it", "refuses a wrong current PIN and changes nothing", "refuses a change without a DM session, even with the right current PIN", "clears the hash, so that setup runs again from localhost only and ends the old sessions", "is the command npm run reset-pin" (spawns `npm run reset-pin`). Guard (`02` §5, G-005): "answers every protected route and every unknown /api path identically without a session, before parsing the body" (every declared route and six unknown paths, six methods, seven request shapes: no body, malformed JSON, schema-shaped body, unknown field, XML, a body over the limit, a stale cookie; status, headers and body compared). Origin (`07` §2): "refuses setup from the server machine with Origin %s and stores nothing" (6 cases), "refuses PIN entry, a PIN change and a sign-out with a foreign Origin, and counts no attempt", "accepts a write whose Origin is the host the browser addressed, and one without an Origin". Hash (G-008): "answers GET /api/settings with the strict settings and nothing else", "carries the hash in no /api response". Logs (`07` §8, G-006): "leaves the earlier failed-PIN lines in the retained files after a flood of rejected requests from one address" (3,000 rejected requests with a 16 KB log file), its control "would lose them without the limit, so the test above can fail", "never writes a PIN, a session identifier or the cookie to the log file or the console". Start-up (`09` §2): "tells the DM to open the DM view on the server PC while no PIN is set, and says nothing once one is". Units: `server/src/auth/auth-units.test.ts`, `server/src/log/limiter.test.ts`, `shared/src/auth.test.ts`. `server/src/http/errors.test.ts` and `app.test.ts` now run with a DM session and check that a request without one gets 401. Mutations, each reverted, each turning named tests red: counting a failure only after hashing (the parallel-guess test: 20 of 20 checked); the cookie without HttpOnly and with SameSite=Lax; the session check skipped for declared routes (8 tests); no Origin check (7); no loopback check (7); a PIN change that keeps other sessions; `SELECT *` for settings (the strict-settings test; the response schema still kept the hash out); no doubling. Live, against `npm start` with a temporary data directory on port 3917: the console printed the `pin.unset` hint; `make smoke` exit 0 (now checks `GET /api/auth` and a 401 without a session); over real HTTP, setup from 127.0.0.1 gave `200 {"dm":true}` and the cookie `Path=/; Max-Age=34560000; HttpOnly; SameSite=Strict`; `GET /api/settings` with it returned the five settings fields; HEAD without it 401; a foreign Origin 403; `npm run reset-pin` cleared the PIN and `GET /api/setup` then answered `{"pin_set":false,"local":true}`; the log file and console held neither the PIN, the session identifier nor the cookie name. |
| SRV-03 | 1 | Campaigns, sessions and scenes over REST | `03` §7, `03` §5 | done | 2026-09-24, pull request from branch `srv-03-campaigns-rest`. Review-fix commit `925a436`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35999345702 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Earlier red: https://github.com/giorgosroussos/dnd-vtt/actions/runs/35996274898 (commit `32e5b02`), 22 of 23 jobs passed; `test · windows` failed on the 5 s default timeout of the live-scene deletion test (5.7 s: three campaign trees in one test on the Windows runner), now split into one test per target under a 60 s file timeout as in `auth.test.ts`. Review (prompt 2), 2026-09-24: no critical or high finding; one low fixed (a new or duplicated scene took `count(*)` as its order and answered 500 when the stored order had a gap; now `max + 1`, test "appends after a gap left outside these routes instead of colliding, and a later rewrite closes it", red against the old query), idempotency tests added (repeated deletion, repeated reorder, duplicating the live scene); the rest deferred as G-014. `make verify` exit 0: lint, format-check, typecheck, `make test` (470 Vitest tests after the review and timeout fixes), `make e2e` (7 Playwright tests), build, check-docs; `make tripwire` exit 0. `server/src/http/campaigns.test.ts` (28 tests against a real SQLite file): "creates, reads, renames and lists campaigns, with ids the server generates as lowercase UUIDs", the same for sessions and scenes, "refuses a body naming its own id, or with an unknown field, and stores nothing", "answers 404 for an unknown entity or parent and 400 for an id that is not a lowercase UUID, storing nothing", "reorders the scenes of a session in two steps and leaves the order unique and gap-free" (a one-step rewrite shown to trip UNIQUE), the same for sessions, "refuses a reorder naming a foreign, missing or repeated id, or leaving one out, and changes nothing", "copies the grid preset of its map image", "stores 30 × 20 for a scene without a map, and the same defaults for a map whose image has no preset yet", "refuses a map image that does not exist and stores nothing", "creates a new scene right after the original with its own copy of every token under new ids", "states how many sessions, scenes and tokens a deletion removes", "deletes nothing without the confirmation, or with one that no longer matches", the scene, session and campaign cascades, "warns that the live scene is among what goes, and clears the live scene when it, its session or its campaign is deleted", "answers every SRV-03 route with 401 and no stored data to a browser without a DM session, such as the player view", "names its columns in every read and write: no server source selects every column or inserts without a column list". `server/src/http/auth.test.ts` "answers every protected route and every unknown /api path identically without a session, before parsing the body" now walks the 21 SRV-03 routes from `app.declaredRoutes`. `shared/src/campaigns.test.ts` (4 tests: strict bodies refuse an id, calendar dates). D-078; Q-090 answered A, campaigns listed by name (D-079); G-012, G-013. |
| SRV-04 | 1 | Image upload pipeline | `05` §6, `05` §7, `03` §7 | done | 2026-09-24, pull request from branch `srv-04-image-upload`. Review-fix commit `4b8c018`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36010683815 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. After the review fixes, locally: `make verify` exit 0 (511 Vitest tests, 7 Playwright tests), `make tripwire` exit 0. Earlier red: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36007512379 (commit `3989675`), 22 of 23 jobs passed; `test · windows` failed with `read ECONNRESET` in the real-connection test of the size limit: the server closed the connection with the body unread, so the client lost the 413. Fixed by D-082 (answer at once, discard the rest of the body, 30 s bound); the test now acts like a browser over a raw socket, sending the whole body before reading the answer, and is red against the old code on Linux as well ("answers 413 over a real connection while the oversized body still arrives, and stores nothing of it", "answers 413 for a declared Content-Length over the limit before the body arrives, over a real connection"). Review (prompt 2), 2026-09-24: correctness, security and isolation, and test passes (no UI, so no UX pass); no critical or high finding. One medium fixed (D-081): a failure while writing the versions was answered 415 "incomplete or corrupt"; "answers 500, not a refusal of the file, when writing a version fails, and leaves no file and no row" is red against the old code. One low fixed: the file route left a file open when reading its size failed. Tests added: "answers not found to a browser that signed out, and to every other browser after a PIN change", "refuses an image with more pixels than the server decodes as 413, storing nothing", "reports an image whose original no longer decodes and keeps its previous display version", and HEAD requests in the no-session test. Deferred: G-017. Before the review, locally (Linux, WSL2, Node 24.11.0, sharp 0.35.4 with libvips 8.18.6): `make verify` exit 0: lint, format-check, typecheck, `make test` (506 Vitest tests in 29 files), `make e2e` (7 Playwright tests), build, check-docs. `server/src/http/images.test.ts` (34 tests against a real SQLite file and images folder, every image generated): "accepts a png|jpeg|webp whatever its Content-Type says, under the lowercase sha256 of its bytes", "refuses a GIF, a GIF named or typed as a PNG, other types and text with 415 naming the type, storing nothing", "refuses a truncated png|jpeg|webp, and one whose signature is followed by garbage, with 415, storing nothing", "refuses an empty upload with 415, storing nothing", "refuses an upload over upload_limit_bytes with 413 naming the limit, storing nothing", "reads the limit from settings on every upload, so a lowered limit applies without a restart", "stops receiving an oversized body while it arrives, before processing, and answers 413 over a real connection" (a chunked body without Content-Length over a real socket; red when the size is checked only at the end), "refuses a declared Content-Length over the limit before reading the body", "returns the stored image and its grid preset for the same bytes, writing no second file", "stores one image when the same bytes are uploaded twice at once", "writes both as WebP, the display version within display_variant_size and the thumbnail at 256 px" (variants validated against `ImageVariantsSchema`, G-009), "never upscales: a small image keeps its size in both versions", "uses the default display size of 4096 px", "stores the size of the image as shown: an EXIF rotation is applied to its size and its versions", "regenerates every display version after the setting changes, leaving originals and thumbnails alone", "leaves no file and no row when storing the row fails after processing", "empties the staging folder and removes image folders without a row when the server starts", "leaves nothing when the client goes away midway through its upload", "reads an image with its preset, and PUT replaces the preset that later scenes copy", "refuses an invalid preset and changes nothing; answers 404 for an unknown image and 400 for a malformed id", "serves every version to a DM session at /images/<sha256>/<variant>", "answers not found to a DM session for an unknown image, variant or path", "answers every request without a DM session, such as the player view, with the same not found, whether the image exists or not" (red without the session check), "removes the map image of a deleted scene with its files and preset once nothing references it", "removes the map images of a deleted session that nothing else references, and keeps one an asset uses", "removes the map images of a deleted campaign that nothing else references" (the three red without the removal, G-013), "keeps a new upload that nothing references yet, and an image whose scene deletion was refused", "declares every SRV-04 route and makes none of them public, so the identical-answer test of auth.test.ts covers them", "refuses an upload without a DM session with 401, and one from a foreign Origin with 403, storing nothing", "answers the metadata and preset routes with 401 and no image data without a DM session". `server/src/http/auth.test.ts` "answers every protected route and every unknown /api path identically without a session, before parsing the body" now walks `POST /api/images`, `GET`/`HEAD /api/images/:id` and `PUT /api/images/:id/preset` too. `shared/src/images.test.ts` (2 tests). |
| SRV-05 | 1 | Asset library over REST | `05` §1, `05` §2, `05` §4, `05` §5 | done | 2026-09-24, pull request https://github.com/giorgosroussos/dnd-vtt/pull/8 from branch `srv-05-asset-library`. Commit `0543978`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36016385776 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Review (prompt 2), 2026-09-24: correctness, security and isolation, and test passes over the diff (no UI, so no UX pass beyond the shape of the refusal the library dialog will show); no critical, high or medium finding. Checked: every route behind the DM-session guard and walked by the identical-answer test; no library field, image id or scene name in any answer without a session, nor in the log line of a refusal (query strings are never logged, D-066); the in-use check, the deletion and the image removal in one transaction; the start-up sweep runs before the server listens. One low accepted, not fixed: `listAssets` groups tags by copying arrays, quadratic in the tags of one asset, immaterial for a personal library. Locally (Linux, WSL2, Node 24.11.0): `make verify` exit 0: lint, format-check, typecheck, `make test` (542 Vitest tests in 31 files), `make e2e` (7 Playwright tests), build, check-docs; `make audit`, `make scan-secrets`, `make tripwire` exit 0. `server/src/http/assets.test.ts` (26 tests against a real SQLite file and images folder, every image generated): "creates, reads, updates and lists assets, with ids the server generates as lowercase UUIDs", "stores every size of the size table and every category", "refuses a body naming its own id, with an unknown field or an invalid value, and stores nothing", "refuses an image that does not exist, on create and on update, and stores nothing", "answers 404 for an unknown asset and 400 for an id that is not a lowercase UUID, changing nothing", "makes a new monster hidden and a new pc, npc or object visible", "takes a default_hidden given at creation over the category, and changes it per asset", "leaves default_hidden alone when the category changes, and every token keeps its own visibility" (D-020, Q-045), "sorts by name, ignoring case", "searches a case-insensitive substring of the name", "searches a substring of the tags too", "narrows by category", "narrows by tags, every selected tag matching, whatever their case", "refuses an unknown query parameter, an unknown category and a blank tag" (D-022, Q-064), "stores one lower-case spelling of a tag, so tags that look the same are one tag", "replaces the whole set of tags on update, keeping the rows of tags that stay" (G-009, D-083), "refuses to delete an asset in use, naming every scene that uses it, and changes nothing" (409 `asset_in_use` with `error.usages`, the same list as `GET /api/assets/:id/usages`, and no scene name in the log), "lists no usage for an asset no token uses", "deletes an unused asset with its tags, and its image with its files once nothing references it", "keeps the image of a deleted asset while another asset or a scene map still uses it", "allows the deletion once the last token of the asset has gone with its scene" (`03` §7, Q-002), "changes the image of every token of the asset, and removes the old image once nothing references it", "keeps the old image while another asset or a scene map uses it, and keeps an unchanged image" (`05` §5), "removes, when the server starts, an upload abandoned before its asset or scene was created, with its files" (G-016, D-084), "declares every SRV-05 route and makes none of them public, so the identical-answer test of auth.test.ts covers them", "answers every SRV-05 route with 401 and no library data to the player view, and serves none of its images". `server/src/http/auth.test.ts` "answers every protected route and every unknown /api path identically without a session, before parsing the body" now walks the nine SRV-05 routes too. `shared/src/assets.test.ts` (4 tests: size table, tag normalisation, body and query schemas); `shared/src/errors.test.ts` "carries the usages of an asset in use, and none otherwise". `server/src/http/images.test.ts` "empties the staging folder and removes image folders without a row when the server starts" now references its kept image from an asset, since D-084 removes an unreferenced one at start. Each of these mutations turns tests red: no tag normalisation (6), OR instead of AND for tags (4), no monster default (3), no in-use check (2), no removal of the old image (1), no start-up sweep (1). |
| PRP-01 | 2 | DM workspace shell | `08` §1, `07` §1 | done | 2026-09-25, both parts merged or green: part 1 pull request #9, part 2 pull request #10. Part 2 review-fix commit `d148553`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36070431322 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Part 2 of 2 (the library panel and the live bar), 2026-09-24, pull request #10 from branch `prp-01-library-livebar`. Commit `dc349aa`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36047778005 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Review (prompt 2), 2026-09-25: correctness, security and isolation, tests, and UX and accessibility passes; no critical, high or medium finding. Checked: thumbnails are fetched only by the DM session and built from the server's sha256; the player view loads none of the library code (`views.spec.ts`); the size check in the browser only spares the DM a wait, the server still judges type and size; an image uploaded for a create that then fails goes at the next start (D-084); every string is in the catalogue. L-1 fixed (D-089): focus was lost after deleting an asset; "moves focus to New asset once the asset it was on is deleted (review L-1)" is red against the old code. L-2 fixed (D-089): the results answering cleared the tag list's failure; "keeps saying the tags could not be read when the results could (review L-2)" is red against the old code. Deferred: no upload progress (G-017, PRP-02). After the fixes, locally: `make verify` exit 0 (598 Vitest tests, 11 Playwright tests). Before the review, locally (Linux, WSL2, Node 24.11.0): `make verify` exit 0: lint, format-check, typecheck, `make test` (596 Vitest tests in 36 files), `make e2e` (11 Playwright tests), build, check-docs; `make audit`, `make scan-secrets`, `make tripwire` exit 0. `client/src/dm/library/Library.test.tsx` (13, jsdom, against the scripted server): "lists by name as the server gives, with the thumbnail, the kind and the hidden marker", "says the library is empty, and that nothing matches a search", "sends the search once typing pauses, and drops an answer that a newer query overtook", "narrows by category and by every selected tag, offering the tags of the whole library", "uploads the image and then creates the asset, leaving default visibility to the category (D-020)", "sends default_hidden when the DM sets it", "refuses a file over the upload limit before sending anything, naming its size and the limit (G-017)", "asks for an image and a name before sending", "shows an upload refused by the server by its message, creating nothing, and allows another try", "keeps Save disabled while the request runs", "edits an asset, keeping its image without a new file and changing it with one (05 §5)", "deletes an unused asset after the confirmation", "lists every scene that uses an asset in use, deletes nothing and offers only Close" (G-014). `client/src/dm/LiveBar.test.tsx` (4): "says nothing is live", "names the live scene", "says nothing is live once the live scene is deleted from the tree (03 §7)", "names the live scene again after it is renamed". `client/src/dm/tree/SceneTree.test.tsx` adds "marks the row a scene is dragged over, and clears it on drop (G-018)" and "keeps Move off in a list while its reorder is sent, so a second press moves from the new order (G-018)". `client/src/ui/messages.test.tsx` adds the workspace with a live scene and an asset, the new-asset dialog and a refused asset deletion. Playwright: `e2e/tests/library.spec.ts` "the DM creates an asset with its image, finds it, edits it and deletes it" (a generated PNG, `e2e/tests/png.ts`; the thumbnail loads; search and a tag filter; the live bar says nothing is live) and "a file over the upload limit is refused before it is sent" (no request reaches `/api/images`); the keyboard gate now operates the search field and the category select (D-088); contrast covers the asset dialog. Each of these mutations turns component tests red: no size check at save, `default_hidden` always sent, usages not shown, the tag filter not sent, Move up enabled while a reorder runs, a stale answer kept (1 each). Part 1 of 2 (sign-in and the scene tree), 2026-09-24, branch `prp-01-signin-tree`; part 2 (library panel, live bar) outstanding, so the package stays `in progress`. Commit `25b3fe7`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36037604454 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Review (prompt 2), 2026-09-24: correctness, security and isolation, tests, and UX and accessibility passes; no critical or high finding. Checked: no form field has a `name`, so a native submit without JavaScript sends no PIN in a query string; the PIN is held only in form state; no stored name reaches the page unescaped; the player view loads none of the DM chunk (`views.spec.ts`); every string is in the catalogue. M-1 fixed (D-087): sign-out showed the PIN form even when `DELETE /api/auth` failed, leaving a valid session on a device that looked signed out; "stays signed in and says so when sign-out fails, so a shared laptop is not left looking signed out (review M-1)" is red against the old code. L-1 fixed (D-087): focus was lost after a confirmed deletion; "moves focus to the parent after a deletion, and to New campaign after a campaign goes (review L-1)" is red against the old code. Deferred: G-018 (no drop indicator, no refresh from other browsers, open branches not kept, Move enabled while a reorder runs). After the fixes, locally: `make verify` exit 0 (575 Vitest tests, 9 Playwright tests); review-fix commit `d344f0d`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36039600337 green on all 23 jobs, read back from the GitHub API; merged as pull request #9. Before the review, locally (Linux, WSL2, Node 24.11.0): `make verify` exit 0: lint, format-check, typecheck, `make test` (573 Vitest tests in 34 files), `make e2e` (9 Playwright tests), build, check-docs; `make audit`, `make scan-secrets`, `make tripwire` exit 0. `client/src/dm/DmView.test.tsx` (10, jsdom, against the scripted server of `client/src/ui/testing/fakeServer.ts`): "offers setup to a browser on the server PC while no PIN exists, and signs it in", "refuses a PIN that is not 4 to 8 digits, or a confirmation that differs, before sending anything", "shows a LAN browser only that setup happens on the server PC, with nothing to fill in", "sends a browser whose setup lost the race to the PIN form", "opens the workspace for the right PIN", "says a PIN is wrong next to the field and clears it", "names the wait of a lockout from Retry-After", "signs out to the PIN form", "returns to the PIN form when the server no longer knows the session", "says it is loading, then offers to try again when the server does not answer". `client/src/dm/tree/SceneTree.test.tsx` (15): "lists campaigns in the order the server gives, by name, and opens each level on demand", "says when there is nothing yet", "selects a scene when its name is chosen", "creates a campaign, a session and a scene, each read back from the server and focused", "keeps Create disabled while its request runs, so a double click makes one entity (G-014)", "refuses an empty name without sending it, and Escape cancels", "renames a campaign, a session and a scene, keeping focus on Rename", "moves a scene down and a session up by keyboard, sending the whole order, with focus kept", "moves a scene by dragging it onto a sibling", "shows the server order and a message when another browser changed the list meanwhile", "offers no reordering for campaigns, which stay by name", "states what a session deletion removes, warns that the live scene is among it, and deletes on confirm", "shows the new counts and deletes nothing when the tree changed after the dialog opened", "cancels without deleting and returns focus to the item", "shows a failure by its catalogue message, never by its code". `client/src/ui/errorMessage.test.ts` "maps every shared error code and `network` to its own catalogue text" (G-014); `client/src/ui/messages.test.tsx` renders setup, setup elsewhere, PIN entry and the empty workspace and finds only catalogue text; `client/src/ui/tokens.test.ts` checks the danger button's pair. Playwright against the production build: `e2e/tests/first-run.spec.ts` "the DM sets the PIN on the server PC, signs out and signs in again" (the First run journey of `08` §10, on the fresh data directory); `e2e/tests/tree.spec.ts` "the DM builds, renames, reorders and deletes in the tree" (drag and Move up, both kept after a reload; Escape cancels a deletion; the dialog's counts; deleting clears the selected scene); `e2e/tests/keyboard.spec.ts` walks the signed-in workspace with a seeded campaign and checks contrast on the PIN form, the workspace, a create form and the delete dialog (D-086). Each of these mutations turns component tests red: the raw code shown (5), deleting without the confirmation (2), Create left enabled while pending (1), a wrong order sent by Move down (1), no confirmation check at setup (1), no return to the PIN form on 401 (1). |
| PRP-02 | 2 | Canvas and grid overlay | `08` §3, `06` §2, `03` §6 | done | 2026-09-25, pull request from branch `prp-02-canvas-grid`. Review-fix commit `2d4a9b2`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36108505364 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Commit `dc16d27`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36076377251 green on all 23 jobs, read back from the GitHub API: `lint`, `format-check`, `typecheck`, `test`, `e2e`, `build`, `check-docs`, `audit`, `scan-secrets` on Linux and Windows, and the five tripwires absent. Review (prompt 2), 2026-09-25: correctness, security and isolation, tests, and UX and accessibility passes. Security: no finding above low (the guard and Origin check precede the body on the new route, an image still in use is never deleted, the player chunks hold no Konva or DM code, nothing leaves the origin). One high, fixed (D-093): keyboard focus fell to the page body after toggling the players' grid or attaching a map, because both controls were disabled while saving; they now refuse through `aria-disabled` and keep focus (`client/src/dm/ScenePanel.test.tsx` "shows the change at once while it is sent, keeping focus on the checkbox, which refuses until the server answers" and "shows the progress of the upload while it is sent, with the controls refusing but focusable, and says when it is done", red with `disabled`; `e2e/tests/canvas.spec.ts` asserts focus after Space). Medium and low findings fixed with tests (D-093): one change at a time; the file refusal beside its field; no endless loading line after a failed map record ("shows a failure to read the map record once, without a loading line that never ends (review UX-4)"); a note beside Replace map; a dark halo under every grid line ("draws each line light over a dark halo, so the grid shows on light maps too (review UX-6)"); layout at 1,024 and 1,280 px; functional camera updates ("keeps every wheel step when several arrive before the next render (review C-2)"); a sideways swipe ignored (review C-1); the display version loaded once per map id (review C-5); `CanvasMap` as the canvas's map prop. Tests added from the test pass: switching scenes while a request runs (`client/src/dm/Workspace.test.tsx`, 2, red without the panel's key), the late record of a replaced map (review L-3), the map-less extent's colour, modifier keys, display-only in the player mode, the real `upload()` over a stand-in XMLHttpRequest (`client/src/dm/api.test.ts`, 7), a rename keeping the grid, a refused replacement keeping the map, two map changes at once, the live scene's map refused without a session until LIV-02; `e2e/tests/canvas.spec.ts` now checks the map-less extent's grey and lines, the wheel anchored at the pointer, and display-only for this map's id. Ten mutations, each reverted, turn these tests red. Deferred: G-020 (LIV-02 revocation test); the Replace map confirmation to PRP-03 (PLAN). Accepted: a scene reselected during its own upload shows its earlier state until selected again; the fake server does not de-duplicate uploads. After the fixes, locally: `make verify` exit 0 (669 Vitest tests in 42 files, 14 Playwright tests). Before the review: 2026-09-25, locally (Linux, WSL2, Node 24.11.0): `make verify` exit 0: lint, format-check, typecheck, `make test` (649 Vitest tests in 40 files), `make e2e` (14 Playwright tests), build, check-docs; `make e2e` then passed three consecutive runs after the keyboard-gate fix (D-092); `make clean-start` exit 0 on the final tree (a fresh copy: `npm ci` from the updated lockfile, migrate, verify with 649 Vitest and 14 Playwright tests, `make smoke` against `make dev`, teardown); `make audit` 0 vulnerabilities; `make scan-secrets` exit 0 and secretlint exit 0 on the untracked files; `make tripwire` all five gates absent. Server, against a real SQLite file and images folder, `server/src/http/scene-setup.test.ts` (13, D-090): "attaches an uploaded map to a map-less scene, starting from the image preset", "starts from the stored defaults for a map without a preset, dropping the grid of the old map", "keeps the grid when the map the scene already has is sent again", "refuses a map image that does not exist and changes nothing", "never moves a token when the map changes (specs/03-domain-model.md §4)", "removes the previous map with its files and preset once nothing references it" (Q-002), "keeps the previous map while another scene or an asset still uses it", "sets grid.visible alone, with a rename, and after a new map in the same body", "changes neither the image preset nor another scene of the same map (specs/03-domain-model.md §5)", "refuses every other grid field, a null map, an unknown field and an empty body, changing nothing", "answers 404 for an unknown scene and 400 for an id that is not a lowercase UUID", and the hidden-information cases "answers a browser without a DM session, such as the player view, with 401 and changes nothing" and "serves the map of a scene that is not live to the DM session only, in every version"; `PATCH /api/scenes/:id` stays in the identical-answer walk of `auth.test.ts`. `shared/src/campaigns.test.ts` "takes a scene map and grid.visible in an update, and refuses every other grid field until PRP-03". Canvas: `client/src/canvas/geometry.test.ts` (10), among them "multiplies size and offsets by display ÷ original width, never reading them as display pixels", "keeps a decimal square size exact, without drifting at the far edge", "reads the stored columns as the width in squares while the map is not calibrated (D-090)", "draws a map-less scene as its columns × rows extent, 30 × 20 by default (specs/03-domain-model.md §6)", and the camera's fit, zoom about a point and keys; `client/src/canvas/MapCanvas.test.tsx` (13, jsdom, the real react-konva node tree): "positions every line from grid.size and offsets times display ÷ original width, never from display pixels", "loads only the display version of the map, never the original (specs/07-security-and-access.md §5)", "draws the overlay faintly in the DM view when players do not see it, and fully when they do (D-026)", "draws no overlay at all in the player view when the grid is hidden for players, and the same one when shown", "draws a map-less scene as its 30 × 20 default extent on a neutral dark background (specs/03-domain-model.md §6)", "starts fitted to the map, in both views", "zooms and pans by keyboard, and a reset fits the map again", "pans by dragging and zooms with the wheel in the DM view", "has no control and nothing that takes focus, and ignores the pointer" (player). DM view: `client/src/dm/ScenePanel.test.tsx` (13): "uploads the file and sets it as the map in the same action, then draws it" (G-016), "refuses a file over the upload limit before sending anything, naming its size and the limit" and "shows the progress of the upload while it is sent, with the controls off, and clears it after" (G-017), "hides and shows the grid for players, and the DM keeps seeing it faintly", "draws the map the server knows, even when the tree held the scene from before it was attached", refusals and network failures; `client/src/ui/messages.test.tsx` "show only catalogue text: a selected scene with its map controls, its canvas and an upload in progress". Playwright against the production build, `e2e/tests/canvas.spec.ts`: "the DM attaches a map, sees it with the overlay, hides the grid for players, zooms and pans, and a reload keeps it" (a generated PNG; the map colour and the overlay read from the canvas pixels, a line on a square edge and none mid-square, fainter once hidden; keyboard, wheel, drag and Fit map; after a reload the map and the setting are back; every `/images/` request ends in `/display`; no request leaves the server's origin), "a map over the upload limit is refused before it is sent", "the canvas of a selected scene is reached, shown and operated by keyboard alone, with readable contrast"; the generic keyboard walk and contrast test still pass. Mutations, each reverted, each turning named tests red: keeping the previous map (1), no grid reset for a map without a preset (1), resetting on the same map (1), grid read in display pixels (8), a faint overlay in the player view (1), loading the original (5), no size check (1), no progress (1), a full overlay for the DM when hidden (2). Not covered: the live scene's change is not pushed to players (G-019, LIV-04); the player view does not mount the canvas yet (LIV-03). |
| PRP-03 | 2 | Grid calibration | `06` §1, `06` §2, `06` §3 | in progress | Pull request from branch `prp-03-grid-calibration`. Commit `ff6f251`: https://github.com/giorgosroussos/dnd-vtt/actions/runs/36115579852 green on all 23 jobs, read back from the GitHub API. Review (prompt 2, four passes, 2026-09-25): one high, eight medium and eight low findings, no critical and no isolation finding; all fixed by D-096 except the live size readout, which D-096 also delivers; CI on the review-fix commit is outstanding, so the package is not `done`. Review fixes, locally: `make verify` exit 0 (727 Vitest tests in 44 files, 18 Playwright tests), `make clean-start` exit 0; new tests among them `scene-setup.test.ts` "refuses a size giving more than 2,000 squares across the map, storing nothing of the body (review)", "attaches a first map to a map-less scene and calibrates it in one body; an unknown map is 400, not 409 (review)", "keeps the preset as the last calibration saved, whichever scene of the map saved it (review)", "answers a repeated identical calibration the same way, changing nothing more (review)"; `MapCanvas.test.tsx` "finishes the rectangle at the last point dragged when the release happens off the stage (review)", "ignores a drag too thin on either axis to be a rectangle (review)"; `Calibration.test.tsx` "refuses to open while a map upload runs, so a draft is never measured on the map being replaced", "keeps one status region mounted through calibrating, so \"Grid calibrated.\" is announced where it appears", "cancels on Escape, sending nothing and returning focus to Calibrate grid"; `calibration.spec.ts` asserts the canvas and the magnifier in view at 1,280 × 720 without scrolling; eight mutations each turn a test red. Implementation, 2026-09-25, locally (Linux, WSL2, Node 24.11.0): `make verify` exit 0: lint, format-check, typecheck, `make test` (708 Vitest tests in 44 files), `make e2e` (18 Playwright tests), build, check-docs; `make e2e` passed two further consecutive runs; `make clean-start` exit 0 (a fresh copy: `npm ci`, migrate, verify with 708 Vitest and 18 Playwright tests, `make smoke` against `make dev`, teardown); `make audit` 0 vulnerabilities; `make scan-secrets` exit 0 and secretlint exit 0 on the untracked files; `make tripwire` all five gates absent. Server, against a real SQLite file, `server/src/http/scene-setup.test.ts` calibration describe (9): "stores a decimal size and the offsets and extent exactly, in original pixels, and writes the same grid as the image preset", "reads a missing size as original width ÷ columns, as the overlay does, so an offset alone calibrates", "starts a new scene of the map from the preset and changes no other existing scene (Q-001)", "keeps the players' grid setting in the preset, and a visibility change alone writes no preset", "calibrates the new map when a body attaches it and calibrates at once, leaving the old map's preset alone", "never moves a token when the calibration changes (specs/03-domain-model.md §4)", "refuses every calibration field on a scene without a map, storing nothing of the body (specs/03-domain-model.md §6)", "writes the preset and the scene in one transaction: a failed scene write leaves the preset as it was", and the hidden-information case "answers a browser without a DM session with 401 and neither the scene nor the preset changes"; "refuses feet per square, the type, a null map, an unknown field and an empty body, changing nothing". `shared/src/campaigns.test.ts` "takes a scene map, grid.visible and the calibration fields in an update (PRP-03, D-094)". Client: `client/src/canvas/calibration.test.ts` (10), among them "gives size = original width ÷ columns, a decimal, with the first line on the edges", "gives size = its side ÷ N and the offset from its corner, within one square", "shows the bottom-right region three squares wide, magnified, with the lines that fall in it", "steps fine-tuning values by the arrow keys without float noise, and keeps the last good calibration for a bad field"; `client/src/canvas/MapCanvas.test.tsx` "draws instead of panning, reports the rectangle in original pixels, and shows it", "shows the measured rectangle it is given, in display pixels, and ignores a click", "pans again once measuring ends, and the player view never measures"; `client/src/dm/calibration/Calibration.test.tsx` (17), among them "offers no calibration on a scene without a map", "draws size = original width ÷ columns as it is typed, and saves it only on Save, in original pixels", "measures a rectangle dragged on the map, converting display to original pixels, then saves size and offset", "steps size and offsets with the arrow keys, 0.1 px or 1 px with Shift, and keeps a decimal size exactly", "draws the original, the calibration version, at the bottom-right corner with the draft grid", "cancels without sending anything, draws the stored grid again and returns focus", "asks first, saying the calibration goes, and changes nothing when cancelled"; three mutations (the overlay drawing the stored grid, the magnifier loading the display version, no replace confirmation) each turned tests red. End to end, `e2e/tests/calibration.spec.ts` (4): "known dimensions calibrate a map live, a reload keeps it, and a new scene of the map starts from its preset", "the rectangle method measures N × N drawn squares dragged on the map, and fine tuning finishes it", "fine tuning keeps a decimal square size exactly, and the overlay lies on a grid drawn at it", "calibration is reached and operated by keyboard alone, with readable contrast, and a calibrated map is replaced only after a confirmation"; the overlay is placed from the canvas pixels within 0.6 screen px of each drawn line, and a draft 0.1 px off failed it. |
| PRP-04 | 2 | Tokens in preparation | `05` §3, `05` §4, `05` §5, `06` §4 | not started | — |
| LIV-01 | 3 | WebSocket rooms, snapshots and reconnection | `04` §1, `04` §5, `04` §6 | not started | — |
| LIV-02 | 3 | Live commands and role projection | `04` §2, `04` §3, `04` §4, `07` §5 | not started | — |
| LIV-03 | 3 | Player view and connecting a screen | `08` §4, `08` §9, `08` §5 | not started | — |
| LIV-04 | 3 | Live and prep modes | `08` §2, `04` §2, `04` §10 | not started | — |
| LIV-05 | 3 | Undo | `04` §8 | not started | — |
| LIV-06 | 3 | Cameras | `04` §9 | not started | — |
| LIV-07 | 3 | Ruler | `06` §5, `04` §11 | not started | — |
| REL-01 | 4 | Operations and repository documents | `09` §1, `09` §2, `09` §5, `09` §6 | not started | — |
| REL-02 | 4 | Acceptance suite and system matrix | `10` §3, `10` §5, `10` §6, `10` §4 | not started | — |
| REL-03 | 4 | Acceptance on the owner's TV | `10` §4, `05` §7 | not started | — |

## Critical end-to-end journeys (`10` §5)

| # | Journey | Status | Evidence |
| --- | --- | --- | --- |
| 1 | First run | not started | — |
| 2 | Prepare | not started | — |
| 3 | Connect TV | not started | — |
| 4 | Run | not started | — |
| 5 | Recover | not started | — |

## Phase exit criteria

Phase exit criteria are in `specs/13-implementation-plan.md` §3–7. A phase is exited only when every package in it is `done` here and its exit criteria have recorded evidence.

- **Phase 0 — exited 2026-09-24.** FND-01 to FND-04 are `done`. Exit criteria (`13` §3): `make clean-start` exit 0 on the tree of commit `59cf2c6` (Linux, WSL2, Node 24.11.0: setup, infra-up, migrate, verify with 277 Vitest and 7 Playwright tests, smoke against `make dev`, teardown); CI green on the remote (https://github.com/giorgosroussos/dnd-vtt/actions/runs/35979443019, 23 of 23 jobs) and a red pipeline blocks a merge (ruleset 23898009 on `main`, FND-02 row); `make check-docs` exit 0; every tripwire still provably absent (the five tripwire jobs of that run); a REST error arrives in the shared envelope (`server/src/http/errors.test.ts`) and a rejected WebSocket command in-process (`server/src/domain/commands.test.ts`), the over-the-wire half belonging to LIV-01 by the owner's decision D-068.
- **Phase 1 — exited 2026-09-24.** SRV-01 to SRV-05 are `done`. Exit criteria (`13` §4): integration tests against a real SQLite file cover every REST resource (`campaigns.test.ts`, `images.test.ts`, `assets.test.ts`, `auth.test.ts`), every deletion rule (cascades and confirmation of campaigns, sessions and scenes, the live scene cleared, the asset in use refused, unreferenced images removed) and every upload rejection (type, size, corrupt, empty); `auth.test.ts` "answers every protected route and every unknown /api path identically without a session, before parsing the body" refuses a request without a DM session on every protected route; CI run 36016385776 green on all 23 jobs.
- Phase 2: entered; PRP-01 and PRP-02 `done`; PRP-03 in progress.
- Phases 2 to 4: not entered.

## Reproducing the evidence

From a clone, with Node 24 or newer on PATH (`nvm use` reads `.nvmrc`):

```bash
make setup        # dependencies from lockfiles, env examples
make infra-up     # no local services; succeeds and says so
make migrate
make verify       # lint, format-check, typecheck, test, e2e, build, check-docs
make dev          # in another terminal; then:
make smoke
make audit        # the CI dependency-scan gate
make scan-secrets # the CI secret-scan gate
make tripwire     # the CI tripwire jobs, all five gates
make check-docs   # needs Python 3 only
```

`make clean-start` runs the same sequence from a fresh environment and tears it down afterwards. Evidence recorded in this file names the command, the date and what it proved; a reviewer must be able to repeat it from this section.
