# GAPS

Deliberate incompleteness and missing infrastructure. A gap is closed only by evidence, never by a stub. Each row names the evidence that closes it and the plan item that produces it. IDs are never reused: a closed gap's row is removed and its ID retired. `make check-docs` verifies that every cited work package and phase exists.

| ID | Gap | Consequence | Evidence to close | Plan item |
| --- | --- | --- | --- | --- |
| G-001 | FND-01 delivered the command contract and the scaffold only: the two views are empty mount points, the server serves them and nothing else (no REST resource, no WebSocket, no schema, no logging beyond two console lines), and there is no CI. Every work package after FND-01 in `specs/13-implementation-plan.md` is unstarted. | No product behaviour in the specifications is verifiable yet; only the gates themselves are. | Per-package rows in `TRACEABILITY.md` moving to `done` with command and test evidence, starting with FND-02. | FND-02, then Phase 0 onward |
| G-002 | The model and webOS version of the owner's LG TV, the player view's acceptance device, are not known. | The display-version size cannot be confirmed and the TV acceptance run cannot be planned against a known browser. | The model or webOS version recorded in `docs/inputs/` and the acceptance run of `10` §4 passing on it. | REL-03 |
| G-003 | The command contract has run on Linux only. `make` needs bash, and `scripts/clean-start.sh` uses `setsid` and `tar`, which Windows runs only through Git Bash or WSL and macOS lacks (`setsid`). | A Windows developer or runner may be unable to run `make clean-start` or `make dev`; the server itself is plain Node and not affected. | The FND-02 Windows job running the `Makefile` targets and passing, or a recorded decision on how Windows runs them. | FND-02 |
