# GAPS

Deliberate incompleteness and missing infrastructure. A gap is closed only by evidence, never by a stub. Each row names the evidence that closes it and the plan item that produces it. IDs are never reused: a closed gap's row is removed and its ID retired. `make check-docs` verifies that every cited work package and phase exists.

| ID | Gap | Consequence | Evidence to close | Plan item |
| --- | --- | --- | --- | --- |
| G-001 | FND-01 delivered the command contract and the scaffold; FND-03 added the conventions: the shared error envelope and schema validation for REST, the WebSocket command and event envelopes with the version counter, and the redacting logger. SRV-01 added the schema of the eight entities (migration 0001) and their contract types in `shared`. SRV-02 added the PIN, DM sessions, the lockout and the session guard on every `/api` route, with `/api/auth`, `/api/setup`, `GET /api/settings` and `PUT /api/settings/pin` (D-076). SRV-03 added campaigns, sessions and scenes over REST: CRUD, ordering, duplication and confirmed, cascading deletion (D-078); SRV-04 added the image upload pipeline, image metadata and presets, and the image files, which only a DM session can fetch until LIV-02 adds the players' entitlement (D-080); SRV-05 added the shared asset library over REST: CRUD with lower-case tags, search and filters, usages and the refusal to delete an asset in use, and an image change that every token of the asset follows, and start-up removes images nothing references (D-083, D-084); PRP-02 added a scene's map and whether players see its grid to `PATCH /api/scenes/:id`, and the shared map canvas (D-090); PRP-03 added the calibration fields to the same route, writing the image's preset in the same transaction, and the three calibration methods with the far-corner magnifier in the DM view (D-094); PRP-04 added the tokens of a scene that is not live over REST, numbered per scene with the numbers issued stored by migration 0002 (Q-091), and the token layer, the picker, placing, dragging with snapping and the token bar in the DM view; a hidden token is numbered only when first shown (Q-092) (D-099, D-100, D-101). LIV-01 added the WebSocket: Socket.io on the server's own port, the `dm` and `players` rooms from the DM session alone, the Origin check on the handshake, a snapshot for each room on connection and on request after a gap, at the version of the state, automatic reconnection in both views, and the sockets of an ended session dropped at once (D-104). LIV-02 registered the live commands `token.add`, `token.move`, `token.setVisibility`, `token.delete`, `scene.activate` and `scene.deactivate` with their payload schemas, applied last-write-wins and projected per room (visible tokens only for players, reveal as `token.added`, hide as `token.removed`), and the players' entitlement to the live scene's display images, read per request (D-109, D-110); `camera.setPlayer`, the ruler and `undo` are still refused as `command_unsupported` (LIV-05 to LIV-07), and the DM view offers no live control and applies no event (LIV-04). LIV-03 made the player view draw the live scene through the canvas's player mode and keep it in step from the players' events, return to the idle screen on `scene.cleared` and hide the pointer after two seconds still; added `GET /api/connect`, the "Connect a screen" panel in the DM view's live bar and the console's URL and QR code at start; and gave both views a loading and a load-failure state from the entry chunk (D-112, D-113). FND-04 made the views shells: the DM view at `/dm` is a banner, a skip link and a main landmark with no workspace (PRP-01). The CI pipeline of FND-02 runs every gate on Linux and Windows. Every work package after LIV-01 in `specs/13-implementation-plan.md` is unstarted. | No product behaviour in the specifications is verifiable yet; only the gates and the conventions are. | Per-package rows in `TRACEABILITY.md` moving to `done` with command and test evidence. | Phase 1 onward |
| G-002 | The model and webOS version of the owner's LG TV, the player view's acceptance device, are not known. | The display-version size cannot be confirmed and the TV acceptance run cannot be planned against a known browser. | The model or webOS version recorded in `docs/inputs/` and the acceptance run of `10` §4 passing on it. | REL-03 |
| G-003 | On Windows, CI runs the gate targets (`lint` to `scan-secrets`) through MSYS2's `make` and bash and they pass (FND-02, D-060), but `make dev` and `make clean-start` are not in CI: `scripts/clean-start.sh` uses `setsid` and `tar`, which Windows has only through MSYS2, Git Bash or WSL, and macOS lacks `setsid`. | A Windows developer may be unable to run `make clean-start` or `make dev` as written; the server itself is plain Node and runs natively (the Windows `e2e` job starts it with `npm start`). | A recorded run of `make dev` and `make clean-start` on Windows, or a recorded decision on how Windows runs them. | REL-02 |
| G-008 | Every database backup (`09` §2) keeps the PIN hash that was current when it was taken, including one from before a `reset-pin` or a PIN change. SRV-02 closed the other half: `settings` is read through explicit column lists, `GET /api/settings` is serialised through the strict `SettingsSchema`, and `server/src/http/auth.test.ts` "carries the hash in no /api response" (D-076). Found by the SRV-01 review, 2026-09-24. | A copied data directory or backup carries old hashes, crackable offline for a 4–8 digit PIN. | The README (REL-01) states that backups hold the PIN hash and how to remove old ones. | REL-01 |
| G-010 | The lockout of `07` §6 is per client address (Q-009), so a LAN client that changes its IPv4 address gets 5 fresh guesses per address: about 1,250 in the first minute from a /24, and a 4-digit PIN within hours. Nothing bounds failures across addresses. IPv6 cannot reach the server today (it listens on `0.0.0.0`); if it ever listens on `::`, keying by the full /128 would make the lockout trivial to bypass. Found by the SRV-02 review, 2026-09-24. | A determined device on the Wi-Fi can still guess a short PIN in one evening. | An owner decision (a question card: a server-wide failure budget, IPv6 keyed by /64, or the accepted residual risk stated in the README), then its test. | REL-01 |
| G-012 | Deleting the live scene, or a session or campaign holding it, clears `settings.live_scene_id` (SRV-03, D-078), but no WebSocket exists yet, so nothing tells a connected player view to show the idle screen (`03` §7, `08` §4). Since LIV-02 the socket exists, but the deletion still sends nothing, and a `scene.deactivate` sent afterwards is acknowledged and sends nothing either, because the live scene is already cleared and deactivating with nothing live tells nobody (D-109); found by the LIV-02 review (correctness pass), 2026-09-26. | Since LIV-03 the player view draws the live scene, so a TV keeps showing a deleted scene until it reconnects or reloads. Meanwhile the DM's Blank TV escape hatch cannot clear a TV showing a deleted scene; reloading the TV page does. | LIV-04 sends the idle state to both rooms when a REST deletion clears the live scene, with a test that a connected player view switches to the idle screen and receives no id of the deleted scene. The same test shows that a DM who deletes the live scene leaves no TV showing it, whether or not a deactivation follows. | LIV-04 |
| G-015 | The display size can change only in the database: `regenerateDisplayVersions` (SRV-04, D-080) rewrites every display version and has its own test, but no route changes `display_variant_size` or starts it, and an upload that is being processed while it runs keeps the size it read when it started. | Until REL-01, a DM cannot tune the display size for the TV (`05` §7, G-002) without editing the database. | REL-01's settings screen changes the setting, starts the regeneration in the background and regenerates an image uploaded during the run, with a test. | REL-01 |
| G-017 | Upload residuals the SRV-04 review deferred, each open only to a DM session or to the DM's own PC. A browser sends the whole body before it reads the answer, so a DM uploading an oversized file waits for all of it to be sent and discarded before seeing the 413 (D-082), and a body still arriving 30 s after the refusal has its connection cut, which the browser shows as a network error; an upload sent with `Expect: 100-continue` and a declared length over the limit is told to continue by Node before the route refuses it. Nothing bounds concurrent uploads or the total size of the images folder, and a PNG under sharp's pixel limit (about 268 megapixels) decodes line by line but still costs CPU. A second server started on the same data directory empties `images/.incoming` at start-up (`prepareImagesDir`) and fails the first server's uploads in progress, before it fails to listen on the taken port. While an asset's image is being sent the library's dialog shows only a disabled Save, with no progress (PRP-01 part 2 review, D-089); the scene map, where files are large, now shows its progress and is checked against the limit before sending (PRP-02, D-090, `client/src/dm/ScenePanel.test.tsx` "shows the progress of the upload while it is sent, with the controls off, and clears it after" and "refuses a file over the upload limit before sending anything, naming its size and the limit"). Found by the SRV-04 review, 2026-09-24. | On a slow link an oversized map takes long to refuse, or ends in a network error; a DM can fill the disk only by uploading; two servers on one data directory lose an upload in flight, not stored data. | The asset dialog shows its upload's progress through `upload(file, onProgress)` as the scene map does, with a component test, or a recorded decision that asset images are small enough not to need it; a `checkContinue` handler answering 413 before the body for a declared length over the limit, with a real-connection test; the README (REL-01) states one server per data directory and that the images folder grows with uploads; or a recorded decision that accepts them. | REL-01 |
| G-018 | The DM view follows another browser only when it next reads what changed: the WebSocket carries only the live scene (`04` §1), and the DM view does not yet read what the `dm` room's snapshots say (LIV-01 opens the connection, D-104), so the tree shows a second DM browser's changes after the next action on that list, a scene's tokens those of another browser when the scene is next selected (PRP-04), and the live bar reads the live scene when the workspace opens and after a change in the tree, not when it changes elsewhere (D-088). Which campaigns and sessions are open is not kept across a reload. PRP-01 part 2 closed the drop indicator and Move while a reorder runs (D-088). Found by the PRP-01 part 1 review, 2026-09-24. | A DM preparing from two browsers at once sees a stale tree or live bar until they act or reload; after a reload the tree opens collapsed. | The live bar following the live scene from the `dm` room's snapshots and events, with a test that activating a scene in one DM browser changes another's live bar; the tree kept in step across browsers by a means `04` §1 allows (the WebSocket carries only the live scene), or a recorded decision accepting it; open branches kept, or a recorded decision accepting a collapsed tree after reload. | LIV-04 |
| G-019 | Changing the live scene's map, its grid calibration or whether players see its grid is saved by `PATCH /api/scenes/:id` (PRP-02, PRP-03, D-090, D-094), but no WebSocket exists yet, so nothing pushes the fresh `scene.snapshot` that `04` §10 requires to either room. The same holds for an asset's image or size changed over `PATCH /api/assets/:id` while a visible token of it is on the live scene (`03` §7, `05` §5): since LIV-02 the players' entitlement follows the asset at once, so the image a TV holds is refused, but nothing tells the TV of the new image or size (LIV-02 review, correctness pass, 2026-09-26). | Since LIV-03 the player view draws the live scene, so a TV keeps the old map, calibration or grid setting until it reconnects, and shows a broken or stale token at its old size until it reconnects. | LIV-04 sends a fresh snapshot to both rooms when the live scene's map, calibration or grid visibility changes over REST, with a test that a connected player view receives the new map, calibration and grid setting and no hidden token. The same snapshot, with its test, when the asset of a token of the live scene changes its image or size. | LIV-04 |
| G-021 | Feet per square cannot be changed: `PATCH /api/scenes/:id` refuses `grid.feet_per_square` and no screen sets it, so every scene keeps the stored 5 and a calibration copies that into the image's preset (D-094). Nothing reads it before the ruler. | A map drawn at 10 ft a square cannot be recorded as such until LIV-07; no stored distance is wrong meanwhile, because none is measured. | LIV-07 adds `feet_per_square` to the scene update and the DM view, with a test that the ruler's distance follows it and that a calibration's preset carries it. | LIV-07 |
| G-022 | At 1,024 px wide the DM workspace leaves the scene column about 320 px: the map file field overflows into the library panel, and the calibration panel wraps into so many rows that the canvas starts below the fold. At 1,280 × 720 the calibration strip, the whole canvas and the magnifier fit without scrolling, which `e2e/tests/calibration.spec.ts` asserts (D-096). The overflow predates PRP-03; found while checking the calibration layout, 2026-09-25. Outside calibration, at 1,280 × 720 the scene's setup rows put the canvas's top at 489 px; PRP-04's token controls share the canvas toolbar's row (D-100), so that is unchanged while no token is selected, and a selected token's actions add one row (528 px) (`e2e/tests/tokens.spec.ts` asserts at most 560 px with long labels), so the page still scrolls to show all of the canvas's 15rem minimum, and at 1,024 × 700 it starts below the fold; `e2e/tests/tokens.spec.ts` scrolls the canvas into view before using the pointer. | In a laptop browser 1,024 px wide (`08` §7 names no minimum), the DM scrolls to reach the canvas while calibrating and sees the file field over the library; at 1,280 × 720 the DM scrolls to see the lower part of the canvas while preparing tokens. | A layout check at 1,024 × 700 in an end-to-end test showing no overlap and the canvas in view while calibrating, and at 1,280 × 720 the canvas in view while preparing tokens, or a recorded decision on the narrowest supported width and height. | REL-02 |
| G-024 | The DM view offers the token controls on every scene, the live one included; the server refuses every token write to the live scene as 409 `scene_live` (D-100), and the view shows that message and reads the tokens again. Since LIV-02 a scene becomes live through the `scene.activate` command, which the DM view does not send yet, so the refusal is reachable only from another client's socket. | Until LIV-04's live mode replaces the controls on the live scene with the live commands, a DM who prepares the live scene is refused after acting instead of being told before. | LIV-04 shows the live scene in live mode, sending the live commands, with a test that no REST token write is made on the live scene from the DM view. | LIV-04 |
| G-026 | A view whose device sleeps may keep a dead WebSocket until Socket.io's heartbeat notices it: the defaults (`pingInterval` 25 s, `pingTimeout` 20 s) are unchanged, and nothing reconnects when the browser reports it is online again or the tab becomes visible, so after waking the TV can take up to about 45 s to resynchronise and the DM's live bar does not say it is reconnecting meanwhile (D-104). Found by the LIV-01 review (correctness pass), 2026-09-25; not reproduced with a real sleep. | The Recover journey (`08` §10) succeeds, but late: a TV woken during play can show a stale picture for most of a minute. | A shorter heartbeat, or a check on `online` and `visibilitychange` that reconnects a dead socket, with a test that a client whose transport stopped answering reconnects within a stated bound; or a recorded decision that the default is acceptable, checked on the owner's TV. | REL-02 |
| G-027 | Signing in again in one tab ends that browser's current session and drops its other DM sockets (D-076, D-104); if another tab's reconnection handshake leaves before the browser has stored the new cookie, it carries the ended session, lands in `players`, and that tab shows the PIN form although the browser holds a valid session. Found by the LIV-01 review (correctness pass), 2026-09-25; not reproduced, the window being the time between the sign-in's answer and the cookie being stored. | A DM using two tabs of one browser may be asked for the PIN once more in the other tab. | The DM view confirms with `GET /api/auth`, or reconnects once, before treating a `players` snapshot as signed out, with a test; or a recorded decision accepting it. | LIV-04 |
| G-028 | The recorded-traffic test of `10` §3 (`server/src/ws/hidden-information.test.ts`, LIV-02) covers adding hidden tokens, revealing, hiding, moving, deleting, activating another scene and reconnecting, and proves the count by comparing with a session without hidden tokens, but not undo, which does not exist before LIV-05; so it carries no `@gate:hidden-information` marker and that tripwire stays in CI (D-110). | An undo that restored a hidden token by a players' event, or leaked its id in an inverse, would not be caught by the gate until LIV-05 adds undo to the script. | LIV-05 adds undo of each token command, of a hidden token included, to the script, marks the test `@gate:hidden-information` and promotes the tripwire. | LIV-05 |
| G-029 | Nothing bounds the player sockets one LAN address may open, and a broadcast (an activation's snapshot, D-109) is encoded once per socket, so a client opening many player sockets makes each activation and each connection cost CPU in proportion (memory stays bounded: an unread socket is dropped, D-106). Found by the LIV-02 review (security pass), 2026-09-26; not measured. | A device on the Wi-Fi opening hundreds of sockets could slow activations for everyone; it learns nothing. | A cap on sockets per address, or one encoding per room broadcast, with a test; or a recorded decision accepting it for a home LAN. | REL-02 |
| G-030 | A revealed token reaches players at its place in the stacking order among visible tokens (D-109), while a token added live always goes on top, so a `token.added` below the top tells players that token lay in the stack before the ones above it, that is, that it was on the scene before it was shown. It names no hidden token that stays hidden and gives no count. Found by the LIV-02 review (security pass), 2026-09-26. | A curious player reading the traffic can tell a revealed token from a newly placed one. | An owner decision: accept it, or have a reveal bring the token to the front (a change to how `04` §4's stacking order applies), with a test either way. | LIV-04 |
| G-031 | Connect a screen residuals the LIV-03 review deferred (2026-09-26). The prominent address is the first private-range one in the system's order (Q-053), which on a PC with Hyper-V, WSL or Docker can be a virtual adapter's (for example `172.17.176.1` on the Windows CI runner came second only by chance), so the QR code may point where the TV cannot reach; the other addresses are listed below it. The URL in the panel is text, selectable with a mouse but not copied by any control. The start-up log records every non-internal address, public or VPN ones included (D-112). | A DM whose PC has virtual adapters may have to type another listed address instead of scanning the code. | An owner decision (a question card: prefer 192.168/16, then 10/8, then 172.16/12, or skip known virtual adapter names) with its test, or the residual accepted in a recorded decision; checked on the owner's PC and TV. | REL-02 |
